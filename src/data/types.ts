export type Account = {
  id: string;
  name: string;
  balance: number;
  color: string;
  icon: string;
};

export type Alert = {
  id: string;
  type: string;
  message: string;
  date: string;
};
